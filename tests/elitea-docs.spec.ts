import { test, expect } from '@playwright/test';

test.describe('ELITEA site navigation', () => {
  test('go to docs from homepage', async ({ page }) => {
    // TODO: test steps will be added
  });
});
